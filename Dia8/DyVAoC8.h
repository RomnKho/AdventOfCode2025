#ifndef DYVAOC8_H
#define DYVAOC8_H

// Función que busca la distancia minima entre dos junkboxes
// min es la distancia anterior
// Se pone para que la funcion no siempre coja el mismo valor 
void find_min_distance(int v[][3], int min) {
	float dist = 0;
	// Bucles para recorrer todo el vector e ir comparando distancias
	for(int 
	
	
}

#endif
